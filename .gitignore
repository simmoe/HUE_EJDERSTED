# Python
__pycache__/
*.pyc
.venv/

# Node
node_modules/
.svelte-kit/

# Build output (regenerated from frontend/)
backend/static/
backend/var/

# OS
.DS_Store

# Runtime state on the Pi (not credentials, but machine-local)
solar_state.json
power_state.json
fossibot_outbox.json
podcast_state.json
zigbee.db

# Local config (contains credentials)
hue_config.json
spotify_config.json
devices.json
gemini_api_key.txt
hub_globals.json
hub_config.json
hub_config.*.json
garden_lights.json
deploy.*.env

# TLS certs
certs/

# Cursor (local editor) — keep shared rules/skills in git
.cursor/*
!.cursor/rules/
!.cursor/rules/**
!.cursor/skills/
!.cursor/skills/**
