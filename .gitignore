# Python
__pycache__/
*.pyc
.venv/

# Node
node_modules/
.svelte-kit/

# Build output (regenerated from frontend/)
backend/static/

# OS
.DS_Store

# Local config (contains credentials)
hue_config.json
spotify_config.json
devices.json
gemini_api_key.txt
hub_globals.json
hub_config.json
hub_config.*.json
garden_lights.json
deploy.*.env

# TLS certs
certs/

# Cursor (local editor state). Track shared rules so every agent sees them.
.cursor/*
!.cursor/rules/
!.cursor/rules/**
